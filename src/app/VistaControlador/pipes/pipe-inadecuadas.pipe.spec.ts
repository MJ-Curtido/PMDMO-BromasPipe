import { PipeInadecuadasPipe } from './pipe-inadecuadas.pipe';

describe('PipeInadecuadasPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeInadecuadasPipe();
    expect(pipe).toBeTruthy();
  });
});
